// Vercel serverless entry point. The same Express app also runs standalone
// via `npm start` on Render/Railway/Fly if you prefer a long-lived server.
import app from '../src/app.js';

export default app;
