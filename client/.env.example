# Only needed when the frontend and backend are deployed to DIFFERENT domains.
# Leave unset locally: Vite proxies /api to http://localhost:4000 automatically.
# Never put database or Supabase service-role keys here — this file ships to the browser.
VITE_API_URL="https://your-backend.vercel.app"
